
    <menu xmlns:android="http://schemas.android.com/apk/res/android">
   
    <!-- Settings, should always be in the overflow -->
    <!-- goActive -->
        <item android:id="@+id/action_go_active"
          android:icon="@drawable/ic_action_go_active"
          android:title="@string/action_goActive"
          android:showAsAction="ifRoom" />
    <!-- landmarkIt -->
        <item android:id="@+id/action_landmark_it"
          android:icon="@drawable/ic_action_landmark_it"
          android:title="@string/action_landmarkIt"
          android:showAsAction="ifRoom" />
    <!-- search -->
    <item android:id="@+id/action_search"
          android:icon="@drawable/ic_action_search"
          android:title="@string/action_search"
          android:showAsAction="ifRoom" />
    <!-- home -->
    <item android:id="@+id/action_home"
          android:icon="@drawable/ic_action_search"
          android:title="@string/action_home"
          android:showAsAction="never" />
   
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:showAsAction="never" />  
          
    </menu>